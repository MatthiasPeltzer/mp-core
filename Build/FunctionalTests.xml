<?xml version="1.0"?>
<!--
    Functional test suite for the mp_core extension.

    Adapted from typo3/testing-framework's boilerplate. Run it with:

        composer test:functional

    By default the suite uses an SQLite database so it runs without any
    credentials (DDEV / CI). To run against MariaDB/MySQL instead, export the
    usual TYPO3 database env vars before invoking phpunit, e.g.:

        typo3DatabaseDriver=pdo_mysql typo3DatabaseHost=db \
        typo3DatabaseName=func typo3DatabaseUsername=db typo3DatabasePassword=db \
        composer test:functional
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/11.2/phpunit.xsd"
         backupGlobals="true"
         bootstrap="FunctionalTestsBootstrap.php"
         cacheDirectory="../.phpunit.cache"
         cacheResult="false"
         colors="true"
         displayDetailsOnTestsThatTriggerDeprecations="true"
         displayDetailsOnTestsThatTriggerErrors="true"
         displayDetailsOnTestsThatTriggerNotices="true"
         displayDetailsOnTestsThatTriggerWarnings="true"
         failOnDeprecation="false"
         failOnNotice="true"
         failOnRisky="true"
         failOnWarning="true"
         requireCoverageMetadata="false"
>
    <testsuites>
        <testsuite name="functional">
            <directory>../Tests/Functional/</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>../Classes</directory>
        </include>
    </source>
    <php>
        <ini name="display_errors" value="1"/>
        <env name="TYPO3_CONTEXT" value="Testing"/>
        <!-- Sensible default DB; does not override an already-exported driver. -->
        <env name="typo3DatabaseDriver" value="pdo_sqlite" force="false"/>
    </php>
</phpunit>
