.DS_Store
/Build/node_modules/
/auth.json
/sync-sitesets-to-vendor.ps1
/sync-sitesets-to-vendor.sh
/webpage/
/Configuration/Services.local.yaml

# Standalone test build (composer install inside the extension) + test caches
/.Build/
/.phpunit.cache/
/.php-cs-fixer.cache
/composer.lock

# Safety net: the standalone install now uses web-dir .Build/Public (ignored via
# /.Build/ above), but keep ignoring a root-level docroot in case one is scaffolded
# locally with a different/overridden web-dir. Never shipped.
/public/
